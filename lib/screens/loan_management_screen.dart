import 'package:flutter/material.dart';

class LoanManagementScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Gestión de Préstamos")),
      body: Center(child: Text("Aqui irá la gestión de los préstamos y los pagos")),
    );
  }
}